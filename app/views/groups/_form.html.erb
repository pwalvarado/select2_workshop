<%= form_for @group, :html => { :class => "form-horizontal group" } do |f| %>

    <% if @group.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @group.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :schedule, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :schedule, :class => 'form-control' %>
    </div>
    <%= error_span(@group[:schedule]) %>
  </div>
  <div class="control-group">
    <%= f.label :course_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :course_id, :class => 'form-control' %>
    </div>
    <%= error_span(@group[:course_id]) %>
  </div>
  <div class="control-group">
    <%= f.label :quota, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :quota, :class => 'form-control' %>
    </div>
    <%= error_span(@group[:quota]) %>
  </div>
  <div class="control-group">
    <%= f.label :enrolled, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :enrolled, :class => 'form-control' %>
    </div>
    <%= error_span(@group[:enrolled]) %>
  </div>
  <div class="control-group">
    <%= f.label :minimun, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :minimun, :class => 'form-control' %>
    </div>
    <%= error_span(@group[:minimun]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            groups_path, :class => 'btn btn-default' %>

<% end %>
